{
  "schemaVersion": "1.0",
  "project": {
    "name": "No Provider Test",
    "description": "Fixture with absent provider declaration — must produce required-provider validation failure"
  },
  "sources": {
    "consumer": {
      "required": false,
      "paths": [
        "frontend/src/api/reservations.js"
      ]
    }
  },
  "commands": {
    "allTests": {
      "program": "npm",
      "args": ["test"]
    }
  },
  "artifacts": {
    "directory": "artifacts"
  }
}
